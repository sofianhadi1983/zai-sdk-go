# Z.ai SDK Configuration
# Copy this file to .env and fill in your values
# Note: .env files are ignored by git for security

# Required: Your Z.ai API key
# Get your API key from:
#   - Overseas: https://docs.z.ai/
#   - China: https://www.bigmodel.cn/
ZAI_API_KEY=your-api-key-here

# Optional: Base URL for the API
# Defaults:
#   - International: https://api.z.ai/api/paas/v4
#   - China: https://open.bigmodel.cn/api/paas/v4
# ZAI_BASE_URL=https://api.z.ai/api/paas/v4

# Optional: Request timeout in seconds (default: 120)
# ZAI_TIMEOUT=120

# Optional: Maximum number of retry attempts (default: 3)
# ZAI_MAX_RETRIES=3

# Optional: Disable JWT token caching (default: true)
# Set to false to enable token caching for better performance
# ZAI_DISABLE_TOKEN_CACHE=true

# Optional: Source channel identifier (default: go-sdk)
# Used for request tracking and analytics
# ZAI_SOURCE_CHANNEL=go-sdk
